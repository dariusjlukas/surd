{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions, opening external links in the system browser, and opening the bundled offline docs in their own window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "opener:allow-open-url",
    "core:webview:allow-create-webview-window",
    "core:window:allow-set-focus"
  ]
}
